# План реализации SEO-аудита aiethos.ru

Источник: `claude-seo-main/aiethos.ru-audit` (аудит от 1 августа 2026, оценка 56/100).
Состояние сверено с продом 2 августа 2026 — часть пунктов уже закрыта, часть отчёт
описывает неточно. Ниже — только то, что реально нужно делать.

---

## 0. Что уже закрыто попутными правками

Эти находки аудита можно вычёркивать — они устранены при работе над мобильной
версией и анимациями, отдельных задач не требуют.

| Находка аудита | Что сделано |
|---|---|
| 5.2 LCP-изображение захотлинкано с `images.unsplash.com`, 2.7 c на 22 КБ, без `width`/`height` | Все контентные фото идут через `next/image`: AVIF 3.2 КБ вместо 22 КБ JPEG, явные размеры, `priority` на первом экране, blur-up плейсхолдер считается на сервере (`lib/lqip.ts`) |
| 7.2 Ни одного `width`/`height`, нет `srcset` и `loading` | Закрыто тем же переходом на `next/image` |
| CLS на мобильных | Раскладка переехала с JS-брейкпоинтов на CSS-переменные — верстка корректна с первого кадра, скачка десктоп→мобайл больше нет |
| 2.2 Статус партнёра заявлен, но не подтверждён | Формулировка «официальный/сертифицированный партнёр Битрикс24 уровня enterprise» убрана со всех страниц, непроверяемое «подтверждённая экспертиза, не самопровозглашённая» удалено |

⚠️ Осталась смежная проблема: сейчас написано «сертифицированный интегратор».
Слово «сертифицированный» — тоже проверяемое утверждение. Нужно решение (см. вопрос №1).

---

## Фаза 1. Техническая база — ✅ ВЫПОЛНЕНА

Всё сделано и проверено на проде 2 августа 2026. Ниже — что было и что стало.

| # | Задача | Результат |
|---|---|---|
| 1.1 | robots.txt | ✅ `app/robots.ts`, 200, с директивой Sitemap и Host. `/privacy` намеренно НЕ закрыт: страница отдаёт `noindex` мета-тегом, а запрет обхода помешал бы роботу его увидеть |
| 1.2 | sitemap.xml | ✅ `app/sitemap.ts`, 200, 4 URL с приоритетами |
| 1.3 | BreadcrumbList | ✅ абсолютные URL на `/ai`, `/bitrix24`, `/contacts` |
| 1.4 | og:image | ✅ `app/opengraph-image.tsx`, 1200×630 PNG, кириллица через TTF из репозитория (satori не умеет woff2, сборка не зависит от сети). `twitter:card=summary_large_image` |
| 1.5 | Шрифты | ✅ `next/font/google`, `@import` убран. Из внешних доменов остался только Метрика. 13 файлов woff2, 308 КБ, раздаются со своего домена. 217 ссылок на семейства переведены на CSS-переменные |
| 1.6 | title / description | ✅ 45/56/65/55 символов (было 62/75/81), description 138–162 (было до 334) |
| 1.7 | Organization + WebSite | ✅ в `@graph` с реквизитами ИП, телефоном, адресом, logo и contactPoint. `ProfessionalService` заменён на `Organization` — подтип `LocalBusiness` без физического офиса был некорректен |
| 1.8 | CTA «Позвоните» | ✅ телефон появился и стоит первой кнопкой в блоке контактов |
| 1.9 | Перелинковка | ✅ ключевые анкоры в теле текста: «внедряем Битрикс24 под ключ», «ИИ-агентов для бизнеса», ссылка на Битрикс24 из шага интеграции на `/ai` |
| 1.10 | favicon / manifest | ✅ `/favicon.ico` 200 (был 404), `/apple-icon.png` 180×180, `/manifest.webmanifest` |
| 1.11 | Русская 404 | ✅ `app/not-found.tsx` с навигацией и своей metadata |
| 1.12 | Метрика | ✅ `preconnect` + `dns-prefetch` к `mc.yandex.ru`, стратегия `lazyOnload` |
| 1.13 | Speculation Rules | ✅ prefetch `/bitrix24`, `/ai`, `/contacts` |
| 1.14 | HTTP/2 | ✅ ALPN отдаёт `h2`, `http_version=2` (был 1.1) |
| 1.15 | Security-заголовки | ✅ HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, X-Frame-Options |
| 1.16 | 301 с www | ✅ `https://www` → 301, `http://www` → 301 (был 404), `http://` → 301 |
| 1.17 | Дубль Cache-Control | ✅ заголовок отдаётся один раз |

Проверено также: домострой (`aicards`) не затронут — сервисы active, порты 8000/8080/8443 отвечают.
Конфиг nginx применён с бэкапом и автооткатом по `nginx -t`.

**Что осталось из аудита:** Вебмастер и Search Console (пункт 2.11) — нужны действия
владельца, см. «Что мне нужно от вас».

<details>
<summary>Исходный план Фазы 1 (до выполнения)</summary>

### Фаза 1. Техническая база — не требует информации от заказчика

Всё делается в коде и в конфиге nginx. Оценка — рабочее время, без согласований.

| # | Задача | Файл | Время |
|---|---|---|---|
| 1.1 | `robots.txt` через `app/robots.ts` + директива `Sitemap`. AI-краулеры (GPTBot, ClaudeBot, PerplexityBot) не закрывать — они дают цитирование | `app/robots.ts` | 20 мин |
| 1.2 | `sitemap.xml` через `app/sitemap.ts` | `app/sitemap.ts` | 20 мин |
| 1.3 | BreadcrumbList → абсолютные URL. Сейчас `"item":"/ai"` — Google отбрасывает разметку целиком | `app/ai/page.jsx`, `app/bitrix24/page.jsx` | 15 мин |
| 1.4 | `og:image` 1200×630 через `app/opengraph-image.tsx` + `twitter:card=summary_large_image`. Сейчас ссылки в Telegram и WhatsApp разворачиваются без картинки — а это единственные каналы связи | `app/opengraph-image.tsx`, `app/layout.jsx` | 2 ч |
| 1.5 | Шрифты на `next/font/google`, убрать `@import` из `globals.css`. Сейчас цепочка HTML → CSS → CSS Google → шрифты, минимум 2 лишних round-trip до текста. Плюс снимается зависимость от доступности Google Fonts в РФ и передача IP посетителей Google (в политике не описана) | `app/layout.jsx`, `app/globals.css` | 2 ч |
| 1.6 | Сократить title: `/ai` 75 → ~56 симв., `/bitrix24` 81 → ~66. Все три description до 150–160 | metadata страниц | 1 ч |
| 1.7 | `Organization` + `WebSite` в `@graph`; в `ProfessionalService` добавить `url`, `@id`, `logo`. Либо заменить `ProfessionalService` на `Organization` — подтип `LocalBusiness` без физического адреса некорректен | `app/layout.jsx` | 2 ч |
| 1.8 | Убрать «Позвоните» из CTA на главной, пока нет телефона | `components/MainPage.jsx` | 10 мин |
| 1.9 | Перелинковка: ключевые анкоры («внедрение Битрикс24 под ключ», «поддержка по SLA») и перекрёстные ссылки `/ai` ↔ `/bitrix24` в теле текста, а не только в навигации | компоненты страниц | 2 ч |
| 1.10 | `app/favicon.ico`, `app/apple-icon.png`, `app/manifest.ts`. Сейчас `/favicon.ico` отдаёт 404, а Яндекс запрашивает именно его | App Router | 40 мин |
| 1.11 | Русская 404: `app/not-found.tsx`. Сейчас англоязычная заглушка Next.js | `app/not-found.tsx` | 40 мин |
| 1.12 | `preconnect` к `mc.yandex.ru`; Метрику на `strategy="lazyOnload"`; Вебвизор включать периодами, а не постоянно (сейчас `tag.js` = 271 КБ стороннего JS) | `app/layout.jsx` | 30 мин |
| 1.13 | Speculation Rules: prefetch/prerender `/ai` и `/bitrix24`. При трёх страницах даёт почти мгновенный переход по главному пути | `app/layout.jsx` | 30 мин |
| 1.14 | nginx: включить HTTP/2 (сейчас ALPN не отдаёт `h2`, всё грузится по HTTP/1.1) | `deploy/nginx-aiethos.conf` | 30 мин |
| 1.15 | nginx: security-заголовки — HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy. Сейчас нет ни одного | `deploy/nginx-aiethos.conf` | 1 ч |
| 1.16 | nginx: 301 с `www` на без-`www` для http и https. Сейчас `https://www.` отдаёт 200 с копией главной, а `http://www.` — 404 | `deploy/nginx-aiethos.conf` | 30 мин |
| 1.17 | nginx: убрать дублирующийся `Cache-Control` на `/_next/static/` — заголовок ставят и Next.js, и nginx | `deploy/nginx-aiethos.conf` | 10 мин |

**Итого фазы: ~15 часов.** После неё сайт технически чист, дальше эффект даёт только контент.

</details>

Отдельно: перед началом правок снять слепок `/seo drift baseline https://aiethos.ru/`,
чтобы ловить случайные регрессии после каждого деплоя.

---

## Фаза 2. Доверие — блокируется информацией от заказчика

Самый крупный блок аудита и, по его же оценке, главный барьер конверсии:
на сайте, продающем внедрения за сотни тысяч рублей, нет ни телефона, ни e-mail,
ни реквизитов. В политике конфиденциальности прямо написано, что реквизиты
появятся «после регистрации».

| # | Задача | Нужны данные |
|---|---|---|
| 2.1 | Страница `/contacts`: юрлицо/ИП, ИНН, e-mail, телефон `tel:`, регион, время ответа. Те же данные — в футер всех страниц | Вопросы 2–4 |
| 2.2 | Реквизиты оператора персональных данных в политике вместо «будут указаны после регистрации». Это требование 152-ФЗ — сайт собирает обращения и ставит Метрику с Вебвизором | Вопросы 2–3 |
| 2.3 | `sameAs` в разметке: каталог интеграторов, Telegram-канал, профили на площадках | Вопрос 5 |
| 2.4 | `/cases` — 3–5 внедрений: отрасль, что было, что сделали, измеримый результат, срок. Обезличенные формулировки («сеть аптек, 40 пользователей») работают, если есть конкретика | Вопрос 6 |
| 2.5 | Блок о команде: роли, опыт, сертификаты | Вопрос 7 |
| 2.6 | Заменить стоковые фото на собственные скриншоты порталов и результатов работы ИИ | Вопрос 8 |

---

## Фаза 3. Контент и охват — снимает потолок в 3 страницы

Сейчас индексируются три страницы. Услуги, которые заслуживают отдельных
посадочных, живут как h2-блоки внутри двух страниц; ответы на вопросные запросы
спрятаны в аккордеонах FAQ.

| # | Задача | Время |
|---|---|---|
| 3.1 | Отдельные посадочные: внедрение · поддержка по SLA · миграция с amoCRM · интеграция с 1С · ИИ-агенты · карточки товаров | 20 ч |
| 3.2 | Блог: 5 статей-ответов на вопросы из текущего FAQ, каждая с h1-вопросом и ответом в первых 130–170 словах — оптимальный формат самодостаточного пассажа для AI-выдачи | 15 ч |
| 3.3 | Блок «кому подходит» на главной: отрасли, размер компании, бюджеты (сейчас главная — 693 слова, тонковата) | 3 ч |
| 3.4 | `offers` с `PriceSpecification` в `Service` для блоков с ценами | 1 ч |

---

## Фаза 4. Мониторинг

| Регулярность | Действие |
|---|---|
| После каждого деплоя | `/seo drift compare https://aiethos.ru/` |
| Еженедельно | Индексация в Яндекс.Вебмастере и GSC |
| Еженедельно | Core Web Vitals в Метрике: «Мониторинг → Скорость загрузки» |
| Ежемесячно | Позиции по кластерам «внедрение битрикс24», «ии-агенты для бизнеса» |
| Ежеквартально | Повторный `/seo audit` |

---

## Что даст наибольший эффект

1. **Фаза 1 целиком** — 15 часов, не требует ни одного согласования, закрывает
   всю индексационную обвязку и скорость.
2. **Контакты и реквизиты (2.1–2.2)** — не SEO-фактор в узком смысле, но самый
   крупный барьер конверсии. B2B-покупатель со средним чеком в сотни тысяч не
   пишет в WhatsApp компании без юрлица.
3. **Отдельные страницы под услуги и вопросы (3.1–3.2)** — три страницы физически
   не могут ранжироваться по десяткам запросов, под которые текст уже написан.

---

## Отдельно: коллизия бренда

По запросу «aiethos» выдача занята американской компанией AIEthos (aiethos.ai),
которая работает как раз в теме GEO и активно публикует пресс-релизы. Брендовый
поиск будет уводить к ней. Аудит предлагает более различимое брендирование в
title — например «ETHOS — интегратор Битрикс24». Требует решения (вопрос 9).

## Отдельно: что аудит рекомендует НЕ делать

- **`llms.txt`** — 404, и это нормально. Ни один крупный AI-поиск его не
  использует; ресурсы разумнее вложить в sitemap.xml и разметку сущностей.
- **`FAQPage`** — Google прекратил показ FAQ-сниппетов 7 мая 2026, Яндекс
  разметку не поддерживает. Оставить как есть, приоритет нулевой: она не вредит
  и полезна AI-ассистентам, но расширенного сниппета не будет.
