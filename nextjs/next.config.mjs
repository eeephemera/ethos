/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Оптимизатор next/image ходит только по явно разрешённым хостам —
    // без этого удалённые картинки отдаются 400-й ошибкой.
    remotePatterns: [{ protocol: 'https', hostname: 'images.unsplash.com' }],
    // AVIF даёт ещё ~20% к WebP; порядок = приоритет при согласовании формата.
    formats: ['image/avif', 'image/webp'],
    // Демо-карточка показывает фото ~180 px шириной; мелкие размеры экономят
    // трафик на мобильных без потери резкости на 2–3x экранах.
    imageSizes: [16, 32, 48, 64, 96, 128, 180, 256, 384],
    // Плейсхолдеры и оптимизированные варианты не меняются — держим их в кэше
    // подольше, чтобы не гонять sharp на каждый запрос (30 дней).
    minimumCacheTTL: 2592000,
  },
};

export default nextConfig;
